import { EventListPopupDirective } from './event-list-popup.directive';

describe('EventListPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new EventListPopupDirective();
    expect(directive).toBeTruthy();
  });
});
