import { RolesInDirective } from './roles.in.directive';

describe('RolesDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesInDirective();
    expect(directive).toBeTruthy();
  });
});
