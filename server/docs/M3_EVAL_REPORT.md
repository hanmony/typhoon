# M3 评估报告（步骤 14，2026-08-19）

> 评估对象：`case-matcher`（步骤 11）+ `alert-analyzer` 研判编排（步骤 12–13）。
> 数据基础：本地 MongoDB（schooltyphoon）6 案例 / 946 事件 / 7 路径（722 点）。
> 执行方式：`node scripts/m3-eval.js`（直接评估，可复跑）+ 2 场全链路 HTTP 抽查（SSE）。
> 口径说明：M3 无空间计算，"受影响线路"字段由 M4 填充；"响应等级建议"由 LLM 文字给出，真模型验收待部署环境。

## 一、10 组场景结果（m3-eval.js 11/11 通过）

| # | 场景 | 结果 | 判定 |
|---|---|---|---|
| S1 | 2022梅花完整路径自匹配 | Top-1=2022梅花 score=1.0 | ✅ |
| S2 | 2021烟花完整路径自匹配 | Top-1=2021烟花 score=1.0 | ✅ |
| S3 | 贝碧嘉完整路径自匹配 | Top-1=贝碧嘉 score=1.0 | ✅ |
| S4 | 普拉桑完整路径自匹配 | Top-1=普拉桑 score=1.0 | ✅ |
| S5 | 2022轩岚诺完整路径自匹配 | Top-1=2022轩岚诺 score=1.0 | ✅ |
| S6 | 2021灿都完整路径自匹配 | Top-1=2021灿都 score=1.0 | ✅ |
| S7 | 梅花早期短轨迹（前 1/3） | Top-1=2022梅花 score=0.3 | ✅（已知限制：仅参考） |
| S8 | 梅花东移 10°（陌生台风） | Top-1=普拉桑 score=0.14 < 0.3（无强相似） | ✅ |
| S9 | 仅 power 文本（无 wind_speed） | Top-1=2022梅花 score=1.0（兼容） | ✅ |
| S10 | 上海登陆型合成台风 | Top-3=贝碧嘉/普拉桑/2021烟花（上海相关命中 1） | ✅ |
| — | 防编造 prompt 断言 | system 含"严禁编造/未知无记录"与案例编号引用 | ✅ |

**亮点**：S10 合成"上海登陆型"台风匹配到 **贝碧嘉**（2024 年真实登陆上海的台风）——算法结果与现实一致；S1–S6 全部自匹配满分；S8 陌生台风正确"不认亲"。

## 二、全链路 HTTP 抽查（SSE）

| 场景 | 结果 |
|---|---|
| A：梅花（tfid=202212）完整研判流 | ✅ HTTP 201 → status×3 → analysis（2022梅花@1.0）→ mock LLM 流式 → [DONE] |
| B：不存在的 tfid | ✅ 按设计走 `event: error`（"未找到当前台风：请传入 tfid 或先建立指挥后再研判"），无 [DONE]（错误路径） |

## 三、验收结论（对照计划 B3）

1. **相似案例匹配有意义**：✅ 6/6 自匹配满分；陌生台风无强相似；上海登陆型匹配现实台风。
2. **报告不含编造数据**：✅ 防编造 prompt 规则断言通过；匹配数据全部来自数据库真实路径/事件（无合成来源）；**真 LLM 回答质量的防编造实测留待部署环境（M6）用真实模型验收**（本机默认 mock 模型回答为固定测试文本）。
3. **响应等级建议与预案分级逻辑一致**：⚠️ M3 由 LLM 文字给出，本机 mock 无法验证 → 列入部署环境验收项。
4. **影响线路判断与空间计算一致**：➖ 属 M4 范畴（affectedLines 字段已预留，M4 填充）。

## 四、已知限制（沿用步骤 11 审查结论）

- 短轨迹（S7）生命周期归一化可能误排，相似案例是参考非确定性结论；
- `affectedLines`/`levelSuggestion` 结构化字段 M3 阶段为空/由 LLM 文字给出；
- 本机 LLM 为 mock，研判报告文字质量、等级建议合理性需真模型评估。

## 五、遗留说明

- 评估脚本 `server/scripts/m3-eval.js`（入库，可复跑）；全链路抽查依赖后端 + mock-llm 运行。
- 测试数据：本地开发库 `typhoontwos` 有梅花（tfid=202212）测试记录（由 pathinfos 97 点构造）。
