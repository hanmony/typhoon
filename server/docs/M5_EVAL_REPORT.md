# M5 综合评估报告（步骤 19，2026-08-19）

> 覆盖验收标准 1–6（AGENT_IMPLEMENTATION_PLAN.md）。本机可验项（4 研判一致性 / 5 性能 / 6 回归）已实测；
> 需真 LLM/浏览器/部署环境的项（1–3、4b、人工卡片检查）明确列入部署验收清单。
> 执行：`node scripts/m5-eval.js`（可复跑，需 mock-llm 8123 + 后端 3001 运行）。

## 一、本机实测结果（8/8 通过）

| 验收标准 | 检查项 | 结果 |
|---|---|---|
| 6 回归 | `/chat/stream`（mock LLM） | ✅ HTTP 201 + [DONE] |
| 6 回归 | `/agent/stream`（mock LLM） | ✅ HTTP 201 + [DONE] |
| 6 回归 | `/kb/query/stream`（真实 Embedding + Qdrant） | ✅ HTTP 201 + sources 事件 + [DONE] |
| 5 性能 | 研判流首事件（TTFT）< 3s | ✅ 7ms（mock 口径） |
| 5 性能 | 研判流总时长 < 30s | ✅ 155ms（mock 口径） |
| 4 研判一致性 | 研判流正常完成（analysis + [DONE]） | ✅ |
| 4 研判一致性 | **卡片 affectedLines 每条线路都能在对应风圈等级（7/10/12）的 line-impact 直接空间计算中溯源** | ✅ 21/21 全部命中 |
| 4 研判一致性 | 卡片线路数与空间计算（7 级）一致 | ✅ 21 = 21 |

**过程发现并修复（本地配置）**：m2 分支 `server/.env`（gitignored）的 Embedding 配置原为 M2 联调占位
（BASE_URL 指向 mock 8123、API_KEY=`unused...`），导致 `/kb/query/stream` 404/401；
已从 data 分支静默同步真实 `EMBEDDING_BASE_URL`/`EMBEDDING_API_KEY` 后回归通过。**密钥未打印、未进 git。**

## 二、部署环境验收清单（M6 部署后执行）

| 验收标准 | 内容 | 需要条件 |
|---|---|---|
| 1 工具正确性 | "2024 年有哪些台风影响上海？" → 调用 `get_typhoon_history` 且返回真实台风（如贝碧嘉），不编造 | 真 LLM + 实时台风 API（学校服务器台风接口当前返回结构错误，需修复数据源） |
| 2 数据真实性 | 台风/预警/事件/值班数据与接口一致；查无内容明确"未知/无记录" | 真 LLM |
| 3 指挥上下文 | 无活跃指挥 → "当前无指挥"而非编造 | 真 LLM |
| 4b 等级建议溯源 | 研判报告应急响应等级建议能溯源到预案条款（RAG 引用 [n]） | 真 LLM + 知识库 |
| **人工卡片检查** | 部署后**人工点击一次「一键研判」**：核对真实卡片布局、消息滚动跟随、小屏（窄面板）显示不溢出 | 浏览器 + 部署环境 |

## 三、结论

- 本机可验部分（回归/性能/研判一致性）**8/8 通过**；研判卡片数据与空间计算**完全一致（无编造线路）**。
- 标准 1–3、4b 与人工卡片检查属部署环境验收项（真 LLM/浏览器），已列入清单。
- M5（步骤 18–19）完成：前端入口 + 研判卡片 + 综合评估。
